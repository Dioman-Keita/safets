import{c as a,Q as s,j as n,m as t}from"./chunks/framework.DM-btvs7.js";const h=JSON.parse('{"title":"How It Works","description":"","frontmatter":{},"headers":[],"relativePath":"guide/how-it-works.md","filePath":"guide/how-it-works.md"}'),l={name:"guide/how-it-works.md"};function o(i,e,p,c,r,d){return s(),n("div",null,[...e[0]||(e[0]=[t(`<h1 id="how-it-works" tabindex="-1">How It Works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to &quot;How It Works&quot;">​</a></h1><h2 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to &quot;Architecture&quot;">​</a></h2><p>SafeTS is built on the TypeScript Compiler API — the same engine that powers VSCode&#39;s IntelliSense.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Your project</span></span>
<span class="line"><span>    │</span></span>
<span class="line"><span>    ▼</span></span>
<span class="line"><span>tsconfig.json ──► TypeScript Program</span></span>
<span class="line"><span>                        │</span></span>
<span class="line"><span>                        ▼</span></span>
<span class="line"><span>                   TypeChecker</span></span>
<span class="line"><span>                        │</span></span>
<span class="line"><span>                   AST Walker</span></span>
<span class="line"><span>                        │</span></span>
<span class="line"><span>              ┌─────────┴─────────┐</span></span>
<span class="line"><span>              │                   │</span></span>
<span class="line"><span>         9 Detectors         Fallback AST</span></span>
<span class="line"><span>         (typed mode)        (no tsconfig)</span></span>
<span class="line"><span>              │</span></span>
<span class="line"><span>              ▼</span></span>
<span class="line"><span>         CrashReport[]</span></span>
<span class="line"><span>              │</span></span>
<span class="line"><span>         ┌────┴────┐</span></span>
<span class="line"><span>         │         │</span></span>
<span class="line"><span>       doctor     debt</span></span>
<span class="line"><span>       fix      baseline</span></span></code></pre></div><h2 id="typed-mode-vs-fallback-mode" tabindex="-1">Typed mode vs Fallback mode <a class="header-anchor" href="#typed-mode-vs-fallback-mode" aria-label="Permalink to &quot;Typed mode vs Fallback mode&quot;">​</a></h2><p><strong>Typed mode</strong> (default when <code>tsconfig.json</code> is found):</p><ul><li>Full TypeChecker available</li><li>Knows the exact type at every AST node</li><li>Can detect nullable access, stale narrowing after await, etc.</li></ul><p><strong>Fallback mode</strong> (when TypeChecker is unusable):</p><ul><li>AST-only analysis</li><li>Detects structural patterns: <code>JSON.parse</code> without try/catch, <code>process.env.X!</code></li><li>Lower confidence, partial results</li></ul><p>SafeTS always tells you which mode it&#39;s running in.</p><h2 id="the-deduplication-system" tabindex="-1">The deduplication system <a class="header-anchor" href="#the-deduplication-system" aria-label="Permalink to &quot;The deduplication system&quot;">​</a></h2><p>For a chain like <code>user.profile.email</code>:</p><ul><li><code>user</code> is <code>User | undefined</code> — report HERE</li><li><code>user.profile</code> — skip, parent already reported</li><li><code>user.profile.email</code> — skip, root already reported</li></ul><p>SafeTS reports only the first nullable node in a chain, not every downstream access. This keeps the report clean and actionable.</p><h2 id="test-file-exclusion" tabindex="-1">Test file exclusion <a class="header-anchor" href="#test-file-exclusion" aria-label="Permalink to &quot;Test file exclusion&quot;">​</a></h2><p>By default SafeTS excludes:</p><ul><li><code>*.test.ts</code>, <code>*.spec.ts</code></li><li><code>/__tests__/</code>, <code>/test/</code>, <code>/tests/</code>, <code>/test-utils/</code></li></ul><p>Test files use deliberate <code>!</code> assertions and <code>JSON.parse</code> without guards as part of test infrastructure. Including them would produce mostly noise.</p><p>Use <code>--include-tests</code> to scan them explicitly.</p><h2 id="bundle-detection" tabindex="-1">Bundle detection <a class="header-anchor" href="#bundle-detection" aria-label="Permalink to &quot;Bundle detection&quot;">​</a></h2><p>Files over 5000 lines are automatically skipped. This prevents SafeTS from scanning compiled bundles, Prisma-generated clients, or other generated artifacts that would produce thousands of false positives on unreachable lines.</p>`,21)])])}const m=a(l,[["render",o]]);export{h as __pageData,m as default};
